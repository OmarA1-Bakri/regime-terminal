{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {"builder": "DOCKERFILE", "dockerfilePath": "Dockerfile"},
  "deploy": {"startCommand": "uvicorn src.api:app --host 0.0.0.0 --port ${PORT:-8080}", "restartPolicyType": "ON_FAILURE"}
}
